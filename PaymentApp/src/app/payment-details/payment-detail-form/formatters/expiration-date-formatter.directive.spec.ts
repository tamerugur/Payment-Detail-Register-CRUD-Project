import { ExpirationDateFormatterDirective } from './expiration-date-formatter.directive';

describe('ExpirationDateFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpirationDateFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
