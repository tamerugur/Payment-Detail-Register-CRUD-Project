import { CvvFormatterDirective } from './cvv-formatter.directive';

describe('CvvFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CvvFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
